/**
 * @param {import('./env-types').TemplateEnv} env
 * @returns {string[]}
 */
function getMessages(env) {
  return []
}

function getMetadata() {
  return {
    version: 10,
    type: 'ongoing-game'
  }
}
